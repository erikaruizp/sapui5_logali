<core:FragmentDefinition xmlns="sap.m"
                         xmlns:core="sap.ui.core"
                         xmlns:f="sap.ui.layout.form">                        
    <Panel>
        <Button id="btnShowCity" text="{i18n>btnShowCity}" press="onShowCity" visible="{jsonConfig>/visibleBtnShowCity}" class="sapUiSmallMarginEnd"/> 
        <Button id="btnHideCity" text="{i18n>btnHideCity}" press="onHideCity" visible="{jsonConfig>/visibleBtnHideCity}" class="sapUiSmallMarginEnd"/>                            
        <Table id="tableEmployee"
                headerText="{i18n>tableEmployeeTitle}" 
                class="sapUiResponsiveMargin" 
                width="auto" 
                items="{jsonEmployee>/Employees}">                                     
            <columns>
                <Column visible="{jsonConfig>/visibleID}">
                    <header>
                    <Label text="{i18n>columnEmployeeID}" />
                    </header>
                </Column>
                <Column visible="{jsonConfig>/visibleName}">
                    <header>
                    <Label text="{i18n>columnEmployeeName}" />
                    </header>
                </Column>   
                <Column visible="{jsonConfig>/visibleCountry}" >
                    <header>
                    <Label text="{i18n>columnEmployeeCountry}" />
                    </header>
                </Column>
                <Column visible="{jsonConfig>/visibleCity}">
                    <header>
                    <Label text="{i18n>columnEmployeeCity}" />
                    </header>
                </Column>
                <Column visible="{jsonConfig>/visibleShowDetail}">
                    <header>
                    <Label text="{i18n>columnShowDetail}" />
                    </header>
                </Column>                
            </columns>
            <items>
                <ColumnListItem>
                        <cells>  
                            <Label text="{jsonEmployee>EmployeeID}" />
                            <Label text="{jsonEmployee>LastName}, {jsonEmployee>FirstName}" />
                            <Label text="{jsonEmployee>Country}" />
                            <Label text="{jsonEmployee>City}" />
                            <core:Icon src="sap-icon://show" press="onShowOrders"/>                            
                        </cells>
                </ColumnListItem>
            </items> 
        </Table>
    </Panel>                              
</core:FragmentDefinition>                         